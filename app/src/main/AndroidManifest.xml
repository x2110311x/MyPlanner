<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.asweeney.myplanner">

    <uses-permission
        android:name="android.permission.INTERNET"
        tools:ignore="ManifestOrder" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPlanner">
        <activity
            android:name=".NewTaskActivity"
            android:label="New Task"
            android:theme="@style/Theme.MyPlanner.NoActionBar"
            android:parentActivityName="com.asweeney.myplanner.NavMenuActivity">
        </activity>
        <activity
            android:name=".SignInActivity"
            android:label="@string/title_activity_sign_in"
            android:theme="@style/Theme.MyPlanner.NoActionBar" />
        <activity
            android:name=".NavMenuActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyPlanner.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>